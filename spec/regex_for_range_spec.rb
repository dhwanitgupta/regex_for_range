require 'spec_helper'

describe RegexForRange do
  it 'has a version number' do
    expect(RegexForRange::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
